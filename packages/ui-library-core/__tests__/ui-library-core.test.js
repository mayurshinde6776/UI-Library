'use strict';

const uiLibraryCore = require('..');
const assert = require('assert').strict;

assert.strictEqual(uiLibraryCore(), 'Hello from uiLibraryCore');
console.info('uiLibraryCore tests passed');
